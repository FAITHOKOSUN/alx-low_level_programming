#include "main.h"
#include <stdio.h>

/**
 * main - entry point
 * @argc: counts argument 
 * @argv: array of strings
 * Return: 0
 */

int main(int argc, char *argv[]);
{
	int a;

	for (a = 0; a < argc; a++)
	{
		printf("%s\n" argv[i]);
	}
	return (0);
}	     	
